// ViewClientPage component does not accept any props
// All data comes from the useViewClientPage hook via URL params
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface ViewClientPageProps {
  // No props needed
}
